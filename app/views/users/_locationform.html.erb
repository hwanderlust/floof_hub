<%= form_for @location, as: :user do |f| %>

  <%= f.fields_for :locations do |loc| %>
  <%= loc.label :street_address %>
  <%= loc.text_field :street_address %>
  <br>
  <%= loc.label :city %>
  <%= loc.text_field :city %>
  <br>
  <%= loc.label :state %>
  <%= loc.text_field :state %>
  <br>
  <%= loc.label :country %>
  <%= loc.text_field :country %>
  <% end %>

<% end %>
